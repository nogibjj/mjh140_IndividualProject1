name: Workflows_by_Job
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  venv:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Venv Activation
        run: |
          make venv
      - name: Set up Python 3.8
        uses: actions/setup-python@v1
        with:
          python-version: 3.8
  install:
    runs-on: ubuntu-latest
    steps:
      - name: Install Dependencies
        run: |
          make install
  format:
    runs-on: ubuntu-latest
    steps:
      - name: Format
        run: |
          make format
  pylint:
    runs-on: ubuntu-latest
    steps:
      - name: Pylint
        run: |
          make pylint
  unitTests:
    runs-on: ubuntu-latest
    steps:
      - name: Unit Tests
        run: |
          make test